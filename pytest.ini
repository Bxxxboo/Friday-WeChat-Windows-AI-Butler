[pytest]
# coverage / omit 见 pyproject.toml [tool.coverage.*]
testpaths = tests
pythonpath = .
addopts =
    --ignore=tests/e2e
    --cov=friday
    --cov-report=term-missing:skip-covered
    --cov-fail-under=55
markers =
    e2e: UI end-to-end tests (requires playwright + chromium)
